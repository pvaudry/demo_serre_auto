# Interface domotique — Serre autonome (Flask)

## Lancer en local

```bash
python -m venv .env
source .env/bin/activate  # sous Windows: .venv\Scripts\activate
pip install -r requirements.txt
python app.py
